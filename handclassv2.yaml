game:
  stack_depth: 25
  bet_sizes: [0.5, 1.0, 1.5]

training:
  iterations: 5000
  seed: 42
  output_dir: "./handclass_25bb_v2"
  mccfr_samples: 5000 
  deal_count: 50000
  min_deals_per_class: 50
  max_rejections_per_class: 500000
  pruning: true
  pruning_threshold: -5.0
  pruning_warmup_fraction: 0.30
  abstraction_mode: hand_class_v2   # was: hand_class
  strength_bits: 4                  # NEW (0-4, default 4)
  equity_bits: 4                    # NEW (0-4, default 4)
