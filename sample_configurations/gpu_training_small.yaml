# Small GPU training stack config for quick testing.
# Trains 3 CFVNet models (turn → flop → preflop) using a pre-trained river model.
#
# Usage:
#   cargo run -p poker-solver-trainer --release --features cuda,gpu-training -- \
#     gpu-train-stack -c sample_configurations/gpu_training_small.yaml -o /tmp/small_stack

game:
  initial_stack: 200
  bet_sizes: ["50%", "100%", "a"]
  ref_pot: 100
  ref_stack: 100

river_model:
  path: "local_data/models/river_v7/checkpoint_epoch340"
  hidden_layers: 7
  hidden_size: 768

model:
  hidden_layers: 4
  hidden_size: 256

training:
  train_batch_size: 512
  train_steps_per_batch: 5
  learning_rate: 0.001
  huber_delta: 1.0
  aux_loss_weight: 1.0
  seed: 42

turn:
  num_samples: 50000
  solve_iterations: 500
  batch_size: 50
  reservoir_capacity: 10000
  validation_interval: 5000
  checkpoint_interval: 25000

flop:
  num_samples: 20000
  solve_iterations: 500
  batch_size: 20
  reservoir_capacity: 5000
  validation_interval: 2000
  checkpoint_interval: 10000

preflop:
  num_samples: 10000
  batch_size: 1
  reservoir_capacity: 5000
  validation_interval: 2000
  checkpoint_interval: 5000
